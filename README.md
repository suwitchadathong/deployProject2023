# deployProject2023

command backend
# Run Django
`python .\manage.py runserver`

# Celery Worker
`celery -A mysite worker --loglevel=info`

# Celery Beat
`celery -A mysite beat --loglevel=info`

command frontend
# Run React 
`npm start`
